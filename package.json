{
  "name": "ee-game",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @ee/app dev",
    "build": "pnpm -r build",
    "wasm": "wasm-pack build crates/sim-wasm --target web --out-dir ../../packages/app/src/wasm"
  }
}
