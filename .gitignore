/target
# The standalone measurement crates (docs/scale-parallelism.md) are their
# own workspaces, so each has its own target dir.
tools/*/target/
node_modules/
dist/
*.wasm.d.ts
packages/app/src/wasm/
.DS_Store

room-save.json

.claude/

# Live server state, not source: the rooms directory is a running server's save
# dir (EE_ROOMS). A room file committed here would be one player's world frozen
# into the repo, and would fight every other checkout.
rooms/
room-save.json.migrated
reuse.txt
